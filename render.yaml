services:
  - type: web
    name: almacen
    runtime: python
    buildCommand: pip install -r requirements.txt && python build.py
    startCommand: python -m gunicorn app.app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
